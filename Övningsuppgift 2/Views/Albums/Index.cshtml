

@model IEnumerable<Albums>
@{
    ViewData["Title"] = "Index";
}

    <div class="container p-3">
        <div class="row pt-4">
            <div class="col-6">
                <h1 class="fw-light">Albums List</h1>
            </div>
            <div class="col-6 text-end">
                <a asp-controller="Albums" asp-action="Create" class="btn btn-secondary">
                    <i class="bi bi-plus-circle"></i> &nbsp; Add a New Album
                </a>
            </div>
        </div>
        <br />

        <table class="table table-striped table-dark">
            <thead>
              <tr>
                <th class="h4 fw-light" scope="col">Album Title</th>
                <th class="h4 fw-light" scope="col">Artist</th>
                <th class="h4 fw-light" scope="col">Genre</th>
                <th class="h3 fw-light" class="pl-5"> </th>
              </tr>
            <br />
            </thead>
            <tbody>
             @foreach(var obj in Model)
                {

              <tr>
                <td>                        @obj.Title
                </td>
                <td>                        @obj.Artist
                </td>
                <td>@obj.Genre</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a id="edit" asp-controller="Albums" asp-action="Edit" asp-route-id="@obj.Id" class="btn mx-2"><i class="bi bi-pencil"></i>&nbsp; Edit</a>
                        <a id="delete" asp-controller="Albums" asp-action="Delete" asp-route-id="@obj.Id" class="btn mx-2"><i class="bi bi-trash-fill"></i>&nbsp; Delete</a>

                    </div>
                </td>
              </tr>
              }
            </tbody>
          </table>
    </div>